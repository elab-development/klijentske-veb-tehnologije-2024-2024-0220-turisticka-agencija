import RegisterForm from "../components/register/RegisterForm";

const Register = () => {
  return (
    <section className="w-full min-h-screen flex items-center justify-center px-5 py-10 bg-(--bg-login)">
      <RegisterForm />
    </section>
  );
};

export default Register;
